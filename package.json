{
  "name": "ethurel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "playwright": "^1.63.0",
    "typescript": "^5.5.4",
    "vite": "^6.4.1",
    "vitest": "^5.0.0"
  }
}
